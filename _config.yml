# _config.yml

# Site Settings
title: "epfl.ada.ch"
theme: jekyll-theme-cayman
baseurl: "/sickada"

markdown: kramdown

# Custom CSS for styling
include:
  - assets/css/style.css

plugins:
  - jekyll-seo-tag
  - jekyll-paginate